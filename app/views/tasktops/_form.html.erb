<%= simple_form_for @tasktop, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :name %>
  <%= f.input :weight %>
  <%= f.input :owner %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                tasktops_path, :class => 'btn' %>
  </div>
<% end %>
